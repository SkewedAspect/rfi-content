Content files for RFI: Precursors
=================================

**Note:**

When re-exporting a blender file with changes, be sure to run 
`repairExportedFiles.sh`, as it fixes some syntax errors with the exported
.babylon files.

(Only works on Linux and possibly OSX)
